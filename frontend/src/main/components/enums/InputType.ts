// Values for our Input component. Can support all default HTML types
// but also custom input types we have added to the Input component
export enum InputType {
  TEXT = "text",
  NUMBER = "number",
  PASSWORD = "password",
  CODE = "code",
  PHONE = "phone",
  //... add more types as we need them
}
